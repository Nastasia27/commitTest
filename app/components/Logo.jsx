import { syne } from "../fonts/fonts"

export default function Logo() {
    return(
        <h2 className={` ${syne.className} text-6xl text-[#F7F5F5] font-bold`}>
            COMMIT
        </h2>
    )
}